import React from "react";

// Styles
import styles from "./Footer.module.css";

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <p className={styles.text}>Digital Value © 2022.</p>
        </footer>
    );
};

export default Footer;
